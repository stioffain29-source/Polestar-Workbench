There is a serious mismatch between the Fuel Watch draft displayed in the Workbench and the exported PDF.

This is not an edited-text or unsaved-override problem. The text was already present in the generated draft before export.

The draft displayed:

Executive Summary:
“Fuel pressure was High this week, with disruption spread across several markets...”

Situation:
“The current backdrop is a rising fuel cost environment with Moderate confidence...”

What Happened:
“In Russia, Orenburg region introduced fuel rationing after a Ukrainian refinery strike...”

What Matters:
“The immediate issue is not one single chokepoint but several separate sources of pressure...”

The exported PDF replaced those sections with different, weaker autogenerated text, including:

“Fuel Watch records 7 qualifying incidents...”

“Current, non-potential evidence covers 7 of the reporting period’s 7 qualifying incidents...”

“What Matters: Iran is the primary pressure point...”

ROOT CAUSE TO INVESTIGATE

The draft screen and PDF exporter are consuming different generated-content objects, different report versions or separate generation paths.

Trace the complete data flow for:

• Draft generation
• Draft display
• Report persistence
• PDF export
• Email export

Identify exactly which object and field each stage reads. Do not assume this is a save-timing issue and do not add a superficial delay.

REQUIRED FIX

The generated draft displayed in the Workbench must become the canonical report snapshot.

The PDF exporter must receive and render that exact snapshot. It must not:

• Run a second narrative generator
• Rebuild sections from incident metadata
• Read a legacy generated-content object
• Read a previous report version
• Use fallback text when the draft section already contains text
• Map the section to a differently named legacy field

Create one canonical report-content schema containing:

• executiveSummary
• marketRead
• operationalRead
• regionalHighlights
• situation
• whatHappened
• whatMatters
• implicationsForBusiness
• watchNext
• polestarView

The draft screen, preview, PDF and email must all consume the same canonical snapshot and section keys.

When Export PDF is selected:

1. Capture the report snapshot currently displayed in the draft.
2. Pass that exact snapshot to the PDF renderer.
3. Do not regenerate or reinterpret any narrative section.
4. Preserve the text, paragraph breaks and ordering exactly.
5. Record the report ID and content-version ID used for the export.

Add a guard that blocks export if the displayed draft version and export version do not match.

AUTOMATED TESTS

Add tests proving that:

1. Generated draft text appears unchanged in the PDF.
2. Executive Summary in the draft and PDF matches exactly.
3. Situation in the draft and PDF matches exactly.
4. What Happened in the draft and PDF matches exactly.
5. What Matters in the draft and PDF matches exactly.
6. Preview, PDF and email all use the same content-version ID.
7. Export does not invoke the narrative generator.
8. Export cannot silently fall back to legacy autogenerated text.
9. Reloading the report preserves the same generated draft snapshot.
10. Export fails visibly if the canonical snapshot is missing.

Do not patch the Fuel Watch wording. Fix the duplicate content paths so the PDF exports the report already displayed in the draft.
