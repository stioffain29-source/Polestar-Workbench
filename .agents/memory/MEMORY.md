- [GDELT flashpoint enrichment](gdelt-enrich.md) — additive precision layer on existing flashpoint rows; match against source_titles[]/source_urls[] not just display_title; APAC Demonstrations coverage sparse/uneven so match rate is structurally low (not a bug); use inArray() not sql ANY().
- [Google News URL resolution](google-news-url-resolution.md) — resolve opaque news.google.com redirects into additive resolved_url so the GDELT URL-match can fire; SCOPE the backfill to the consuming topic (flashpoint) or higher-volume topics starve it; new-format ids need the 2-call batchexecute exchange.
- [Liveuamap live-map overlay](liveuamap-overlay.md) — cached server proxy for the PAID Liveuamap overlay; not ingested (licensing/parity); failed fetches surface explicitly; liveuamap.com 403s our egress IP (their Cloudflare block, not a code bug).
- [ReliefWeb situational-reports context](reliefweb-situational-reports.md) — standalone humanitarian reports in OWN table (never incidents); v2 validates request BEFORE appname gate, so date must be second-precision (no ms) + use appname (NOT browser) UA; 406 = HDX bot/egress-IP block (like liveuamap), 403 = appname unapproved.
- [Title translation marker gate](title-translation-markers.md) — foreign incident titles only translate if they hit INDONESIAN_MARKER_WORDS (Bahasa is ASCII so word-list is the only gate) AND AI_INTEGRATIONS_OPENAI_* is provisioned; new region/topic vocab silently ships raw — add distinctly-non-English words, replay the ~* candidate query to verify.
- [LLM reasoning-model token cap](llm-reasoning-token-cap.md) — gpt-5* burn max_completion_tokens on reasoning FIRST; a low cap → empty content, no error → "most rows work, a few ship raw"; keep ALL ingest LLM caps ≥8192.
- [pnpm overrides need lockfile regen](pnpm-overrides-lockfile.md) — editing pnpm-workspace.yaml overrides/catalog without regenerating pnpm-lock.yaml breaks the --frozen-lockfile post-merge install on every future merge.
- [Source Health live-feed telemetry](source-health-live-feeds.md) — ingest self-registers REAL per-feed health into `sources` via recordSourceHealth (key name,topic; non-fatal, commit-gated); old placeholders deleted once via marker-gated boot migration; any new ingest path must call it or its topic shows empty/stale; country coverage gate must keep topic∈{flashpoint,protests}.
- [Feed fetch resilience](feed-fetch-resilience.md) — all scrapers MUST use shared fetchFeed (browser UA + retry + gzip decode), never parser.parseURL; Google News throttles generic-UA concurrent bursts → "timed out after 20000ms" in Source Health (server-side, not user network); retry only transient errors, concurrency 4.
- [Shared relevance engine + persisted gate](relevance-engine.md) — one @workspace/relevance lib feeds ingest+API+frontend; relevance persisted on incidents, central API filter (fail-open NULL, ?includeIrrelevant raw), boot backfill re-cleans on RELEVANCE_RULE_VERSION bump; generic topic reports already gated so persisting≠regression.
- [Flashpoint rally homonym](flashpoint-rally-homonym.md) — political rally vs market/crypto/sports/disaster: EXCLUDE the homonyms FIRST, then KEEP only object-bearing political rallies; add the exclude in lockstep with any new keep cue; bump RELEVANCE_RULE_VERSION; topic-not-geo (US rallies still pass).
- [Conflict relevance excludes](conflict-relevance-excludes.md) — conflict REQUIRED keys off bare naxal/maoist/militant tokens so business/diplomacy noise leaks; fix with tightly-bound CONFLICT_EXCLUDE (gated by violence override) + version bump, not by loosening REQUIRED; broad money/diplomacy words collide with bounties/ceasefires so replay the full set first.
- [Flashpoint weak-operational filters](flashpoint-weak-ops.md) — the noise classes the classifier keeps but the prose builders must drop (stock photo wires, sports-media protest, suspended strikes, court-only legal process, SEO comma-spam, non-APAC syndication).
- [Flashpoint prose contradiction-killers](flashpoint-prose-reconciliation.md) — forecast-table(future) vs country-chart(volume) lead is reconciled not hidden; never call a Low a "severity escalation" (gate rank≥3); Watch Next always tops up to ~6; one shared pickProse for every section incl. Exec Summary in BOTH preview+PDF.
- [Conflict Watch event-led prose](conflict-prose-events.md) — auto-prose cites real events (cleaned headline + date); "deadly" tracks casualtySignalCount NOT severity rank; rewriting auto-prose needs prior signatures in GENERIC_CONFLICT_PHRASES (but absent from NEW auto) or saved reports stay stale; "not country-wide" containment claim gated on hasFocus&&localised in EVERY section incl. Polestar.
- [Headless PDF cover embedding](headless-pdf-cover.md) — `prepareCoverImage` must branch on `typeof Image` so Node tsx exports embed the cover photo instead of falling back to gradient.
- [Openable PDF export](openable-pdf-export.md) — jsPDF headless PDFs won't open (Adobe-Identity-H) AND show last week's window; render live page with system Nix Chromium `page.pdf()` instead, isolating `.print-report` so it paginates.
- [Country dual-severity Fast Facts](country-dual-severity.md) — country brief shows BOTH "Severity This Week" (7d) AND "Standing Risk" (90d, =watchlist WORST) so an active theatre never reads Low in a quiet week; Extreme colour must be #A33232 not #800000.
- [Country report narrative — AI-generated, fingerprint-cached, editable](country-report-data-driven.md) — LLM narrative persisted + fingerprint-cached (regen on data change, 409 on stale edit), template fallback; West Papua mis-tag guard + cross-border exception.
- [Country-report AI prose engine](country-ai-prose.md) — country narrative is LLM-generated from live incidents, fingerprint-cached + editable, template = fallback; the cache fingerprint MUST hash the exact canonical/capped incident set (all rendered fields) the prompt sends, or it bypasses cache (wallet-DoS) and goes stale.
- [Prod incident backfill route](prod-incident-backfill.md) — when feed non-determinism leaves prod missing a genuine dev incident, re-ingest is a coin flip; token-gated POST /api/admin/incidents/backfill copies verbatim records (dedup both keys; normalise +00→+00:00; prod writable only in deployment).
- [In-app prod ingestion trigger](in-app-prod-ingestion.md) — token-gated server route + auto scheduler refresh prod (runtime owns writable DB); staleness = no TRIGGER not stale feeds; only flashpoint/cargo_watch have scrapers; never close shared pool in lib; pg advisory lock not a flag.
- [jsPDF table keep-together pagination](pdf-table-pagination.md) — pre-measure heading+table+disclaimer & break up-front; measure MUST mirror render font/width/row formula or it re-orphans; disclaimer reserve via measureDisclaimerHeight; fuel in-app Download uses jsPDF exportTopicReportPdf (NOT DOM rasterise); headless script = faithful repro.
- [protests vs flashpoint topic split](topic-protests-vs-flashpoint.md) — "Protests & Civil Unrest" monitor must read live `flashpoint`; dead `protests` bucket also stranded real West Papua conflict (scored under wrong rule→hidden), fixed by marker-gated re-home+re-rate.
- [recharts line screenshot artifact](recharts-line-screenshot-artifact.md) — LineCharts draw left-to-right on mount, so fresh-load screenshots look empty on the right (bars don't); disable animation + read browser-log console before assuming a data/width bug.
- [Monitor screenshots as all-zero mid-load](monitor-page-load-zero.md) — large topic monitors (Shipping) default incidents=[] and render cards before isLoading resolves; an early screenshot (esp. during a boot-ingest) reads all 0 though API/DB are full — re-screenshot after load, curl the API, don't edit shared scope logic.
- [Live shipping scraper](shipping-scraper.md) — shipping is now LIVE (was import-only); "port strike" collides with military strikes (funeral/debunk noise → false extreme, DENY-listed); dedupe must grow guard sets in-run because per-feed default country makes same-URL keys differ.
- [Generic live news-topic scrapers](live-news-topic-scrapers.md) — energy/fertiliser/fuel incidents LIVE via config-driven runNewsTopicIngest; strikes now LIVE too but via its OWN module + dedicated early boot-only run (full ingest was timing out before reaching it); new topic must thread severity+types+index+runner+scheduler boot-gate+admin+scripts or paths diverge.
- [Energy relevance precision](energy-relevance-precision.md) — energy gate excludes out-of-region geography BEFORE required (precision-first); deliberately omits bare turkey/russia/europe (collide with legit ME stories; ukraine token already catches the war noise); rule changes need a RELEVANCE_RULE_VERSION bump.
- [Google News rank-cap & targeted feeds](google-news-rank-cap.md) — broad national OR-query is rank-capped (~70-100/feed) so local items drop though ingest is fresh; classify via dry-run "New to insert:0" + country-edition RSS probe; fix = place-anchored edition feed (no version bump); some hyperlocal items aren't on Google News at all.
- [Missile Strike Tracker scraper](strikes-scraper.md) — strikes now LIVE too (own runStrikesIngest, not newsTopic); dedupe is DELIBERATELY conservative single-best-per-cluster (title-split inflated 104→225 via reworded syndication); casualties = explicit deaths only; deny-list groups drop trailing \b so plurals/inflections match.
- [Strike Tracker dashboard derivation](strikes-dashboard-derivation.md) — Strikes.tsx buckets client-side: Target precedence is military-signal→DB-enum→text→Unknown (no "Other"); strikeText excludes source/url; charts show a caveat when Unknown>50% not hidden; 4+4 layout, generic sub-location, no maritime port chart.
- [Shared strike-target rulebook](strike-targets-shared-rulebook.md) — one @workspace/strike-targets lib owns target/infra regexes+classify; ingest writes columns, dashboard builds labels from same signals; add new target words THERE only (leading-\b-only stems) or the two surfaces drift again.
- [Strike stored-vs-rulebook disagreements](strikes-rulebook-disagreement-review.md) — when stored target_category ≠ classifyStrikeTarget, stored is usually RIGHT (regex can't tell attacker/responder/aircraft from target); blanket overwrite UNSAFE, keep backfill blank-only; genuine mis-stores fixed by text-keyed marker-gated UPDATE in migrations.ts.
- [Strikes Unknown-bar reduction](strikes-unknown-reduction.md) — shrink Unknown by filling DB columns (trusted before text); caveat is per-window not whole-table; classifier leading-\b only (refiner→refinery trap); backfill auto-scraped rows only via marker-gated boot; residual = manual seed rows the DISPLAY regex misses.
- [Geocode masthead pollution + theatre clamp](geocode-masthead-pollution.md) — geocode scans title+summary so a source name ("Taipei Times") leaks its city; centroid-distance guard is a no-op when country has no centroid, so fixed-geography topics need a region bounding-box clamp; fix existing prod rows by RELOCATE-not-delete marker-gated boot migration.
- [Report data provenance & PDF parity](report-data-provenance.md) — in-app PDF rasterises the on-screen DOM (screen==PDF free); data-status uses incident dates not the sources table; protests→flashpoint topic mapping; non-destructive stale-prose guard compares by calendar date.
- [Report date tz projection](report-date-timezone-projection.md) — compare a UTC occurredAt vs a bare issue date in UTC (utcYmd), never local format(): an evening-UTC record rolls to the next local day for eastern viewers → FALSE stale banner + mis-clamp; clamp fix fans out to country/fuel/card; window filter & display dates left local on purpose.
- [Live-prod report+PDF proof](live-prod-report-proof.md) — drive prod URL with Playwright (REPLIT_PLAYWRIGHT_CHROMIUM_EXECUTABLE) to click in-app Download PDF; screen==PDF is architectural, so prove prod renders current data.
- [Incident severity classification](incident-severity-classification.md) — severity rated from text via shared classifySeverity (extreme reserved); a lib-level backfill re-rates historical auto-scraped rows because scraper dedupe never re-touches them.
- [Fuel actor/category classification](fuel-actor-classification.md) — a bare institution name (municipality/council/authority) is NOT a policy action; Government category needs institution+lever; supply-arrival/shortage-easing → Market/supply.
- [Fuel severity capping](fuel-severity-capping.md) — fuel PDF severity chip downgrades high/extreme by default; speculative/warning framing (could/warns/forecast) beats a bare operational word, so commentary never reads Extreme; only casualty or concrete physical disruption keeps it.
- [Incident geocoding](incident-geocoding.md) — incidents geocoded from curated lookup table (city→country centroid), not an API; centroid keys must match classifier country aliases or markers silently drop; jitter at render not in DB.
- [News region-feed Unknown country](news-region-feed-unknown-country.md) — multi-country feeds default country='Unknown' (→ "—"); detectCountry only matches COUNTRY_ALIASES so subnational/utility/regulator names drop; fix = expand exported gazetteer + marker-gated Unknown-only backfill (never overwrite attributed rows).
- [Option A static-report window](option-a-static-window.md) — static/import-only reports dated to their data window; clamp issue date at seed AND manual edit; killing a rolling window means purging all "30-day" prose/labels; banner reads spaced in pdftotext.
- [Live fuel prices (FRED)](live-fuel-prices.md) — fuel Brent/WTI/jet are a live FRED ingest into hardNumbers, NOT the old hardcoded sample; rides runIngestOnce; cold-start freshness gated on incident sources.
- [Fuel Tracker disruption panel](fuel-disruption-panel.md) — fuel monitor LEADS with a curated analyst disruption panel (own dataset, "Not reported" fallbacks, intentional not fabrication) ABOVE the live FRED price feed; aviation/jet-fuel relevance must be disruption-bound; bump RELEVANCE_RULE_VERSION on parser changes.
- [Workbench owner-only auth](workbench-token-gate.md) — PRIVATE to the owner via Replit Auth (reversed the old public decision); requireOwner gates all data routers; session reads cookie before Bearer so the admin token can't shadow it.
- [Shipping monitor vs report dedup (decoupled)](shipping-monitor-dedup.md) — monitor collapses vessel rows HARDER than the comprehensive report: report=conservative exact-day dedupeByEventKey, monitor=aggressive coarse {act,region,time} dedupeVesselEventsClustered; cross-day syndication = one event many cards + Extreme&Low; report must not over-merge.
- [Dashboard relevance gate + march-month homonym](dashboard-relevance-gate.md) — dashboard widgets read raw SQL with no gate (relevance is frontend-only → filter client-side + over-fetch); bare "march" matched the calendar month and leaked economics into the unambiguous tier.
- [Shipping Watch report cleanup](shipping-report-cleanup.md) — closure-rhetoric vs confirmed-closure is precision-first; counts use credible-only rows; seed prose from SAME dataset as chart; SAVED prose overrides live until a marker-gated one-time DB reset (every-boot wipe = data loss; app_migration_markers gate).
- [Cargo report country consistency](cargo-report-country-consistency.md) — every country-counting surface (Fast Facts, prose, Reads) must use shared splitAttributedCountries (split compounds, drop Unknown); subset Reads name their scope ("among these route-side records") to explain not contradict the headline lead; gate firm-lead on strong attribution. Also: Country Risk Breakdown table per-country severity escalates AT MOST +1 tier above modal (recur≥2 only) so outliers never paint a mostly-Low country Extreme-red.
- [Cargo Watch USD loss honesty](cargo-watch-usd-honesty.md) — cargo-loss $ parser stays first-match+context-gated+<$100M cap, NO largest/k-shorthand/FX (both inflated May $24.8k→$317k via asset-seizure totals & FX approximations); verify parser changes by replaying over live rows.
- [Cargo ingest scope coverage](cargo-ingest-scope-coverage.md) — "cargo light" = feed COVERAGE gap; keep feed lists, English aliases & LLM SCOPE_CANON mirroring cargoAnalysis scope; classify masthead-STRIPPED title; never cargo-DENY piracy (DENY precedes ALLOW).
- [Related Incidents shared selector + preview parity](related-incidents-selector.md) — selectRelatedIncidents is the one row-selection authority for both preview+PDF (preview previously had NO table = parity gap); cargo source line height must be in measureTable AND render; footer counts cover as page 1.
- [Incident title translation (display_title)](incident-title-translation.md) — non-English headlines → clean English in NULLABLE display_title (original kept); detection predicate IDENTICAL in JS+SQL WHERE (shared consts) so per-run limit never starves on permanent-English NULLs; bound-param regex, `\y` boundary; prod backfill via INGEST_FORCE_VERSION bump.
- [Cargo local-language translation gate](cargo-local-language-translation.md) — cargo_watch in-scope count is English-only (frontend gate); translate by non-Latin-SCRIPT + Indonesian markers, NOT non-ASCII (Bahasa is ASCII→silently dumped to excluded_non_cargo); Drizzle sql escapes: use [:ascii:]/\\y not \x00/\m.
- [PNG flashpoint feeds & Pacific crime acceptance](png-flashpoint-feeds.md) — needs classifier accepting Pacific violent crime + when:14d; a "thin week" is often masthead-stripping dropping pure-local outlet items at country resolution, not sparse reality (authoritative-outlet null-fallback); Bahasa cues still a gap.
- [Flashpoint APAC coverage chain + homonym gates](flashpoint-apac-coverage.md) — adding an APAC country = seed feed + COUNTRY_ALIASES + FP_APAC_ANCHOR_RE + geocode + version bump (any miss silently drops it); interstate diplomatic gate needs CAPITALS (seoul) in its nation list; "march into [sport] semis"/"play football" leak; boot backfill is sequential/uncapped so poll version before concluding it didn't fire.
- [Fuel producer/buyer table sparseness](fuel-producer-buyer-table.md) — sparse actions table = relevance REQUIRED missing "fuel crisis/emergency" + nearDuplicate false-merging distinct carriers on generic crisis tokens (needs Jaccard floor); verify via live-prod replay.
- [Topic monitor date-range toggles](topic-monitor-date-range.md) — shared dateRange/RangeToggle scaffold (default 2y, no lower bound); windowed set drives range-scoped surfaces but FIXED-PERIOD captions (last7/last30) must read the all-time set or they lie.
- [Incidents days window cap](incidents-days-window-cap.md) — /incidents `days` param hard-capped at 365 (sending 2y=730 → 400); table has >1y rows so switching a page from useListIncidents({}) to ({days}) drops old data + makes any all-time "Total" last-year-scoped; no all-time count API.
- [Shipping piracy allowlist gap](piracy-allowlist-gap.md) — Singapore-Strait sea-robbery dropped: piracy/robbery missing from BOTH ingest ALLOW + relevance REQUIRED; fix = maritime-qualified phrases in lockstep, bump RELEVANCE_RULE_VERSION; prove via deterministic samples (feed counts swing).
- [Report prose voice & cadence](report-prose-voice.md) — plain reader-facing prose, no analyst-speak; cadence from MONTHLY_TOPICS set (cargo_watch+fertiliser monthly, rest weekly) flipped in 3 places; never edit GENERIC_FLASHPOINT_PROSE list; keep preview/PDF parity.
- [Report AI analytical narrative](report-ai-narrative.md) — every topic report gets AI prose via OWN report_prose_cache+reportProse.ts (keyed by report id, NOT folded into country_report_prose); structured briefs extend the png country variant; separate prompt-version constants; stripProseCountAnnotations enforces no-count rule at coerceStr/coerceList/coerceJoined; fuel parity is MANUAL (jsPDF builder, not DOM-rasterise).
- [Report "thin content" diagnosis](report-thin-content-diagnosis.md) — "thin" report = thin ReportPack AND a coarse classifier dumping plurality into "Other X" → "Data quality issue" Fast Fact; fix both; verify classifier on relevance-filtered+windowed rows.
- [Spot Report capability](spot-report-capability.md) — enum clear-on-UPDATE; map re-fit gated on point-SET (fitKey); preview==every export; analyst-placed multi-point markers via mapPoints jsonb; photos jsonb (data URLs) render after Incident Details, reorderable.
- [base64 data URLs in JSON bodies](json-body-base64-limits.md) — inline images need BOTH a raised express.json limit (default 100KB→413 before the route) AND route-level count/size/content-type caps + matching client guard.
- [PDF pagination page-fill](pdf-pagination-fill.md) — in-app DOM-rasterise PDF only breaks at structural boundaries → tall prose pushed whole = white space; fix = mark prose data-pdf-flow for line-level breaks + previous-KEPT dedup (line height ≈ guard cascade-drops runs); scope marker to one report.
- [Cloud Run uptime outages](cloudrun-uptime-outages.md) — outage alert with no crash/error + same pid alive = transient blip; min-instances=1 fixes it (takes effect after republish).
- [Reports publish-status reality](report-status-data-reality.md) — reports stay status='draft' (none 'published'); treat issueDate as the publish date; country_slug NULL; country/region come from country+spot reports.
- [Prod schema via boot migrations](prod-schema-via-migrations.md) — a new DB column needs BOTH the Drizzle schema AND an idempotent ALTER…ADD COLUMN IF NOT EXISTS in api-server runDataMigrations; drizzle push only hits dev, prod DATABASE_URL is read-only from workspace.
- [Card visual panel map](card-map-html2canvas.md) — card PNG export CLONES the DOM then html2canvas's it; any card widget must rasterise from static DOM (HTML overlay dots + img tiles, NO canvas/SVG markers) like IncidentMap.
- [html2canvas text clamp + chip centering](html2canvas-text-clamp.md) — html2canvas renders text LOW: line-clamp centre-clips, overflow+maxHeight bottom-shears (fix=content-box+paddingBottom 0.3em), and severity CHIP labels sit low/off-centre — CSS can't fix it, replace the chip with a real <canvas> (textBaseline:middle) at export, scoped via data-attr.
- [Severity chip vertical centering (screen)](chip-vertical-centering.md) — all-caps pills read HIGH when lineHeight==chip-height (centers em-box); fix is lineHeight:1 + SYMMETRIC padding under flex, never an asymmetric top-padding nudge (overshoots low); PDF decoupled via export overrides; measure with Playwright 6x + ImageMagick trim, not Range rects.
- [Card Builder chart embed](card-chart-embed.md) — in-card dashboard graphs MUST be card-native HTML/div bars (html2canvas mangles recharts SVG); auto-filled from FULL datasets (all theatres/all-time), NOT the dashboard's scoped window — strike notes say "All theatres" to keep that honest.
- [Conflict report coverage & prose](conflict-report-coverage-and-prose.md) — Conflict is WEEKLY (not monthly); rank by IMPACT not count; Option-B pulls in pre-window High/casualty theatres as a standing-watch tier sorted BELOW in-window (mixing them contradicts the "most serious this period" headline); kinetic/casualty-bound relevance; lead hotspot phrase ≤2×.
- [Conflict report sub-national hotspots](conflict-report-subnational.md) — incident.location is EMPTY so conflict report localises via curated COUNTRY_HOTSPOTS gazetteer over TITLE text; "rest of country is safe" claims must gate on localised (coverage≥0.5), not bare hasFocus, or one matched title re-fakes containment.
- [Conflict location-led report](conflict-report-location-led.md) — dropping a report section must gate the editor field + save payload + localStorage, not just preview/PDF, else hidden stale prose persists; conflict prose is dynamic per-theatre from shared dataset builder.
- [Conflict relief/peace exclude](conflict-relief-exclude.md) — conflict gate is REQUIRED-only so "ex-rebels do relief" slips in via actor word; narrow relief/peace+disaster exclude runs before REQUIRED but GATED on a hard armed-violence override (bare death words excluded) so an ambushed convoy/collapsed peace process still keeps.
- [Shipping report lead development](shipping-lead-development.md) — name the week's dominant chokepoint headline via most-corroborated (Jaccard) cluster, but key the pool on the chokepoint NAME not detectChokepoints (ops-gate excludes pure political "Hormuz reopen" headlines); Exec Summary is localStorage-only and must honour the staleness/reseed guard or stale local copies bury the lead.
- [Flashpoint title-rescue over-admit + monitor dedup](flashpoint-title-rescue-overadmit.md) — bare protest/crackdown in a HEADLINE re-admits metaphors ("instant protest") & animal-welfare enforcement; fix BEFORE the rescue (HARD title-exclude or gated pre-rescue drop w/ public-gathering override), bump RELEVANCE_RULE_VERSION; monitor/dashboard collapse syndication via resolveFlashpointTrue→exported dedupeByTitle.
- [Monitor syndication dedup (generic)](monitor-syndication-dedup.md) — energy/fuel/fertiliser/conflict/cargo monitors need CONSERVATIVE full-title dedup (NOT flashpoint's fuzzy pass); strip masthead only when the tail looks like a source or distinct dash-subtitles over-merge; keep raw DB tallies un-deduped.
- [Shipping commerce-homonym leak](shipping-commerce-homonyms.md) — port-congestion/freight-rate are commerce not maritime-security; fix by REMOVING the over-broad REQUIRED phrase (precision-first), never a broad exclude; bump RELEVANCE_RULE_VERSION + replay live.
- [Country report window dedup layer](country-report-window-dedup.md) — generic country report didn't dedup syndication (PNG dedups internally → silent divergence); dedup at the computeCountryFastFacts windowIncidents input so table+map+charts+prose agree; cleanIncidentTitle needs a bare-domain branch to strip "voi.id".
- [Flashpoint finance/regulatory crackdown](flashpoint-finance-clampdown.md) — financial "crackdown"/"clampdown" (banks/insurers/investment/capital flows) is a markets story, not unrest; FP_NEG_CRACKDOWN's adjacency window misses spread-out finance vocab so add FP_NEG_FINANCIAL gated on !FP_UNREST_COMPANION; bump RELEVANCE_RULE_VERSION.
- [Flashpoint industrial-action crime-strike homonym](flashpoint-industrial-action-homonym.md) — "copper thieves strike"/metal-theft = property crime, but FLASHPOINT_INDUSTRIAL_ACTION_RE admits it (bare "strike"+industrial anchor); exclude bound to a crime ACTOR not the bare noun "theft" (else legit "workers strike over fuel theft" drops); bump RELEVANCE_RULE_VERSION.
- [Codegen breaks workbench HMR](codegen-vite-hmr-restart.md) — running api-spec codegen while the workbench vite dev server is up corrupts HMR ("Failed to reload *.tsx", partial render, page not scrollable); restart the workbench+api-server workflows before screenshot/e2e; green typecheck ≠ healthy dev server.
- [Jest route-test harness](jest-route-test-harness.md) — bare-express route tests must stub req.log or req.log.* throws→HTML 500→res.json() SyntaxError; *TestHelpers.ts need a testPathIgnorePatterns entry.
- [Workbench server-lib barrel import](workbench-server-lib-barrel-import.md) — client importing the @workspace/ingest ROOT barrel bundles pg/drizzle into the browser → "Buffer is not defined" crash; fix = pure subpath export, never the barrel.
- [jest barrel import.meta breakage](jest-barrel-import-meta.md) — a barrel re-exporting an import.meta module (dev-env loader) makes EVERY ts-jest suite importing it "fail to run" with import.meta SyntaxError (ts-jest emits esnext into CJS node); green typecheck/dev ≠ green jest; keep import.meta helpers out of the root barrel.
- [PNG/West Papua structured country report](png-country-report.md) — config-driven structured brief serves BOTH papua-new-guinea AND papua (West Papua); one row=one theatre's enrichment (WP backfill excludes PNG via NOT ILIKE); structuredTheatre gate ≠ local cross-border filter; per-item attrs derived CLIENT-SIDE; screen==PDF via DOM-rasterise; exact empty-location fallback string.
- [Boot freshness gate covers context layers](boot-freshness-gate-context-layers.md) — anything refreshing ONLY inside runIngestOnce (strikes, land topics, AIS movement) must join the boot gate's OR or it drifts stale while incidents stay fresh; gate each on its provider being ACTIVE + scope to the live feed.
- [ICC CCS / IMB piracy ingest](icc-piracy-ingest.md) — standalone maritime-security source in OWN table (never incidents); custom_field_data is an ARRAY of {id,name,value} (Object.values→"[object Object]" trap); title=incident no., -YY→year, current-year only; tableStats.latest is a string not Date.
- [Per-incident AI summaries in report tables](report-incident-summaries.md) — topic/conflict/shipping Related Incidents rows get a one-line AI summary (preview==PDF via shared resolveIncidentSummary); flashpoint/protests/fuel out of scope; generate from the EXACT rendered related set (≤60 cap) or cache goes stale/bypasses.
- [AIS provider cost switch](ais-provider-cost-switch.md) — paid Datalastic kill-switched off (VESSEL_REGISTRY_ENABLED=false), free aisstream is active; trade-off = zero Gulf/Hormuz coverage (panels read "not reported", not a bug); credential resolves AIS_API_KEY||AISSTREAM_API_KEY via resolveAisKey().
- [maritime-movement scrape ops](maritime-movement-scrape-ops.md) — AIS commit run LOOKS hung but is the inline Datalastic registry pass (set VESSEL_REGISTRY_ENABLED=false for a fast movement-only run); panel fills per-theatre on observed traffic (short samples ≈ Singapore Strait only); pgrep -f self-matches → use bracket trick.
- [Live Fleet Intelligence panel](fleet-intelligence-panel.md) — Shipping monitor AIS aggregation (flags from MMSI MID, composition from ship-type, per-chokepoint counts); real-data answer to the fabricated-mockup-names fury; nav-status unreported→"—" not 0; shares VesselMap query key; destinations/multi-day transits impossible from this feed.
- [Shipping off-region relevance gate](shipping-offregion-relevance.md) — drop off-scope shipping (Black Sea/Baltic/UK shadow-fleet/Baltic Dry Index) ONLY when no tracked theatre named; gate runs before REQUIRED so broad "tanker seized" can't rescue; bump RELEVANCE_RULE_VERSION so boot backfill re-cleans persisted rows.
- [Papua-shows-PNG = stale prose cache](papua-png-stale-cache.md) — "West Papua brief reads as PNG" is usually a lingering pre-guard country_report_prose row, NOT a live leak (exact-token match already blocks pure-PNG); verify the CURRENT cached row + incident set before touching guards; bump PROSE_PROMPT_VERSION to invalidate.
- [Maritime Intelligence shared builder](maritime-intelligence-board.md) — Shipping monitor + Shipping Watch report share ONE buildMaritimeIntelligence; it MUST self-filter topic==="shipping" (report feeds all topics, monitor feeds shipping-only → silent 8-vs-6 divergence); movement (AIS) is context-only in its own admin-uploaded table; Orval zod can't emit .int() so enforce integer in the hand-written route refine.
- [Red Sea directional-flow panel + AIS theatre wiring](red-sea-directional-flow.md) — inbound/outbound = heading split of a live AIS sample (not transits, totals are "observations"), Navy/Electric bars never red, HTML/div bars not recharts; one pure builder → monitor==preview==PDF; flow fetched per-gateway (limit 40) while the board uses the global pool; new AIS theatre must be registered in all 5 surfaces with a byte-identical name or it silently drops.
- [KAMMI social-media protest watch](social-watch-kammi.md) — IG+Telegram posts are additive context in own table (never incidents); only explicit promote creates a flashpoint/Indonesia incident (server re-derives eligibility, 409 if done); confirmed TG channel is inactive (2016 posts) — data reality not a bug; no active KAMMI TG channel exists anywhere, so the only live source is the PAID Apify Instagram feed (needs a real apify_api_ token).
- [Agent secret-store delete quirk](agent-secret-store-delete.md) — deleteEnvVars "succeeds" but can't remove a user-supplied requestEnvVar secret (different layer); verify secret values from a FRESH process, and park bad keys via a config flag (e.g. INSTAGRAM_ENABLED=false) not by deleting.
- [Drizzle aggregate runtime types](drizzle-aggregate-runtime-types.md) — max()/min() over a timestamp are typed Date but return an ISO STRING at runtime; coerce with new Date() before .toISOString() or it throws.
- [Severity-tier color ramp duplication](severity-ramp-duplication.md) — the 5-tier ramp is copy-pasted across ~9 files (RATING_COLORS/SEV_COLOR/CARD_RATING_COLORS/MARITIME_RISK_COLOR); change a tier color in ALL in lockstep, keep per-tier TEXT color in sync (screen RATING_TEXT_COLORS + hidden PDF chip-text ternaries like exportTopicReportPdf), and triage old-hex hits (ramp vs no-data accent vs chart-category palette); A33232=Extreme-only, 1B6B7A=Insignificant-only.
- [Social-watch promote UI refresh](social-watch-promote-ui-refresh.md) — Orval mutations never invalidate their list query; promoted/created/updated rows stay stale unless the component calls queryClient.invalidateQueries(get<Thing>QueryKey()) on success.
- [Facebook OSINT promote gate](facebook-osint-promote-gate.md) — social_raw is isolated CONTEXT, never an incident surface; only an atomic, server-re-derived promote mints an incident (claim/rollback + dedup invariants in topic file).
- [Cargo Needs Review analyst override](cargo-needs-review-override.md) — resolve unidentified-country cargo rows by persisting analystInScope+country; classifyScope honors it ABOVE the cargo-vocab gate but BELOW hard non-cargo/fish rejects, gated to APAC/ME so blank/out-of-scope picks can't leak; picker = IN_SCOPE_COUNTRIES.
- [Country report op-ed/debate leak](country-report-opinion-leak.md) — country report IGNORES server relevance_status (includeIrrelevant + isCountryRelevant is the sole authority); fix "irrelevant item shows" THERE, no version bump; haystack is RAW untranslated title so English-only excludes miss Bahasa op-eds ("untuk siapa").
- [Cargo monitor relevance bypass](cargo-monitor-relevance-bypass.md) — Cargo Watch monitor empty/thin = server's GENERAL relevance gate marking genuine cargo theft 'irrelevant', NOT classifyScope; fix on the frontend fetch (includeIrrelevant + trust classifyScope), no version bump; mirrors CountryReport.
- [Structured brief prose builder](structured-brief-prose-builder.md) — ONE shared deterministic builder serves PNG/West Papua/Indonesia/Jakarta (fix once = all four); route every category label through categoryPhrase() (raw labels for comparison only); never `is ${phrase}` (singular/plural trap); banned jargon: operating tempo/standing baseline/reads this period as.
- [indonesia_local broad-coverage topic](indonesia-local-topic.md) — Bahasa-first topic feeding Jakarta+Indonesia COUNTRY reports (flashpoint stays unrest-only); category NULL→client-side rules; no version bump; WATCH cross-language double-count.
- [Indonesia/Jakarta foreign-subject slop guard](indonesia-foreign-subject-guard.md) — foreign events filed country=Indonesia flood the brief; defect is GEOGRAPHY not topic so fix is a dominance guard in CountryReport (over displayTitle, NOT Bahasa title), not isCountryRelevant; no version bump.
- [Cross-country leak — foreign theatre guard](cross-country-leak-foreign-theatre.md) — country reports leaked others' incidents; mention-count/tag-order/stored-geo all FAIL, named-theatre geography wins; every marker's littorals must be members or that state self-drops.
- [Country report shared renderer + layout controls](country-report-shared-renderer.md) — ONE renderer all countries; fixed section order; 6 themed Incident Details (exhaustive themeForCategory); NO Maritime Security (topic reports keep it); analyst map(7)/photo(6) placement persisted OUTSIDE prose cache so layout edits never regen narrative.
- [Country-report output standard](country-report-output-standard.md) — banned BLUF filler trio; charts OFF by default; map controls off at construction; Papua captions are spec-sourced standing profiles, not stale data.
- [Operating-risk structured briefs](operating-risk-briefs.md) — Indonesia/Jakarta weekly briefs (proseVariant="operating-risk") render DETERMINISTIC prose; pngEffectiveDataset early-returns so the generic AI overlay never overwrites them (no-fabrication; bug only bites AI-keyed PROD); Papua exclusion reads i.title ONLY (masthead "Sabang Merauke NEWS" trap).
- [Country-report fire/explosion wording gate](country-fire-cause-wording.md) — generic fire paragraph is source-safe (never classifies cause); the deliberate/arson sentence gates on `deliberate` (arson/attack only), NOT `.security` (which also counts protest-adjacent context); never loosen it back.
- [CountryReportMap tile-layer lifecycle](country-map-tile-lifecycle.md) — Leaflet tile layer is created ONCE; any theatre-conditional basemap (Jakarta label-free/faded vs standard) must resync via tileLayerRef.setUrl/setOpacity on countryName change, not just branch at construction, or a reused instance inherits the wrong style.
- [Jakarta exposure-map hazard prose](jakarta-exposure-hazard-prose.md) — name a hazard only when reported this period; the headless PDF MAIN EXPOSURE column is the trap, must use live hazard text not the static area.exposure template.
- [Editable report reads via pickRead](editable-report-reads.md) — every topic-report narrative "read" is owner-editable via ONE shared pickRead(override,auto) in BOTH preview+PDF (blank=>generated, no fabrication); never re-define a local pick() (cargo drift); conflict per-theatre reads keyed in conflictAreaReads jsonb.
- [Wire/video cruft title cleaning](wire-cruft-title-cleaning.md) — shared stripWireCruft (incidentTitle.ts) applied at each topic's ONE preview+PDF dataset chokepoint BEFORE dedup (display-only, no version bump); generic country path is computeCountryFastFacts, NOT toItem — the easy miss.
- [Flashpoint video-cruft clean](flashpoint-video-cruft-dedup.md) — clean the rendered title AND the dedup key via ONE fn or a "Watch:" prefix double-counts an event; trailing "VIDEO BY" strip must be capitalised-credit-anchored, never lowercase-greedy (no-fabrication).
