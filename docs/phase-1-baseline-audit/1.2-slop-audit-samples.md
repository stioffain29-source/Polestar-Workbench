# 1.2 — Ingestion slop audit (kept vs dropped)

**Audit date:** 2026-08-26  
**Live prod samples:** Run `runPhase1SlopAudit.ts` → [output/slop-audit-samples.generated.md](./output/slop-audit-samples.generated.md)

**Stakeholder deliverables (MD + DOCX):** [ingestion-audit-kept-vs-dropped.md](./ingestion-audit-kept-vs-dropped.md) · [ingestion-audit-kept-vs-dropped.docx](./ingestion-audit-kept-vs-dropped.docx)

Regenerate: `python scripts/generate_ingestion_audit_report.py` (or with live data: `pnpm --filter workbench run audit:ingestion-report`)

This document catalogs **known slop classes** from institutional memory and the code surfaces that admit or reject them. After running the live script against prod, merge generated rows into the tables below for stakeholder review.

---

## Flashpoint / Protests

| Type | Example pattern | Source | Why wrong / right | Pipeline stage |
| --- | --- | --- | --- | --- |
| **FP** | "Taklimakan Rally" | Client-flagged | Motorsport rally, not civil unrest | Relevance → `FLASHPOINT_EXCLUDE` |
| **FP** | "NBA strike sports betting" | Client-flagged | Sports labour homonym | Relevance |
| **FP** | "Copper thieves strike Auckland train line" | Memory | Property-crime verb "strike", not industrial action | Relevance `FLASHPOINT_EXCLUDE` |
| **FP** | "Market rally" / "crypto rally" / "rally for the peso" | Memory | Finance homonym — exclude must run BEFORE political KEEP cues | Relevance (order load-bearing) |
| **FP** | US MAGA / Trump mass rally | Accepted tradeoff | Topically political; geo filter is scraper `FOREIGN_LOCATION*` in `flashpoint.ts`, not relevance lib | Ingest classify |
| **FN** | Genuine APAC protest dropped as "weak-operational" | Funnel audit | Sports wire, photo caption, court-only aftermath | `selectFlashpointUsable` |
| **FN** | Pakistan / South Korea rows lost at funnel stage | `diagnoseFunnel.ts` focus | Check per-stage: kinetic-only, court-only, duplicate | Second-stage selector |

**Scripts:** `audit:flashpoint`, `diagnoseFunnel.ts`, `replayFlashpointRelevance.ts`, `auditLiveRelevance.ts`

---

## Cargo Watch

| Type | Example pattern | Source | Why wrong / right | Pipeline stage |
| --- | --- | --- | --- | --- |
| **FP** | "Cargo theft costs trucking $18M a day" | `cargoSlop.ts` | Economic commentary, not incident | Relevance + `classifyScope` |
| **FP** | "Safer Transport Act advances" | `cargoSlop.ts` | Legislation process | Relevance exclude |
| **FP** | LAPD / FreightWaves / "cargo theft in Latin America" | `cargoSlop.ts` | US trade-press / out-of-region | Relevance exclude |
| **FP** | Generic warehouse burglary (Indonesia) | Memory | Bare premises word without load context | `cargoAnalysis.hasGenuineCargo` |
| **FP** | SE Asia piracy tagged "China" via SCMP masthead | Memory | Masthead stripped too late in ingest | Ingest `classifyFeedItem` |
| **FN** | Tagalog truck diesel-siphon theft | Memory | English-only gate misses local language | `LOCAL_FEEDS` + LLM screen |
| **FN** | Ten-wheeler ambushed in transit, no named load | Memory | Was dropped as vehicle-theft noise | `TRANSIT_ATTACK_VERB_RE` rescue |
| **FN** | Sri Lanka cargo (English query) | Memory | Feed language gap — ~0 yield expected | Coverage, not classifier |

**Scripts:** `cargoScopeCheck.ts`, `auditReports.ts`, replay `classifyScope` over live rows

---

## Shipping

| Type | Example pattern | Source | Why | Stage |
| --- | --- | --- | --- | --- |
| **FP** | Off-region syndicated maritime news | Memory | Homonym / syndication | `topicRelevance` shipping excludes |
| **FN** | Pure piracy in cargo feed | By design | Routed to shipping unless cargo-related phrase | Ingest ALLOW/DENY order |

**Note:** Monitor dedup ≠ report dedup by design (`shipping-monitor-dedup.md`).

---

## Country briefs (PNG, West Papua, Indonesia, Jakarta, Thailand, Philippines)

| Type | Example pattern | Source | Why | Stage |
| --- | --- | --- | --- | --- |
| **FP** | Foreign earthquake filed `country=Indonesia` | Memory | Topic-relevant but geographically foreign | `isForeignSubjectForIndonesia` (render gate) |
| **FP** | NBA Knicks riot, Japan vs Sweden match | Memory | `displayTitle` English reveals foreign subject; summary masthead false anchor | Country report filter |
| **FP** | Yemen war row, Bahasa title "Yaman"/"Houthi" | Memory | English-only foreign tokens miss untranslated titles | Extend `INDO_FOREIGN_SUBJECT_RE` |
| **FP** | Opinion / editorial leak | Memory | Not operational risk | `country-engine` banned phrases |
| **FN** | Genuine domestic story naming foreign national | Memory | Dominance test must not blanket-drop | `isForeignSubjectForIndonesia` |

**Scripts:** `countryReportData.ts`, `country-brief-sweep` workflow

---

## Cross-topic / infrastructure

| Area | Known noise | Location |
| --- | --- | --- |
| Geocode pollution | Source masthead as `location` | Ingest geocode lookup |
| Region feeds | `country='Unknown'` on subnational items | News region feeds + `COUNTRY_ALIASES` |
| Social promote | Minted incidents without corroboration | Facebook/Instagram/KAMMI promote pass |
| GDELT | Promote/enrich precision | `pnpm --filter @workspace/scripts eval:gdelt` |
| Homonyms (shared) | Commerce vs maritime, off-region syndication | `lib/relevance/src/topicRelevance.ts` |

---

## Review workflow

1. Export prod snapshot: `exportProdIncidentsSnapshot.ts`
2. Generate tables: `runPhase1SlopAudit.ts`
3. For each high-priority topic, manually tag 20–30 rows:
   - **False positive** — slop that got through
   - **False negative** — real signal dropped
4. Share appendix with Steve for Phase 2 prioritisation

**Acceptance:** Stakeholder agrees the sample set represents current pain; no major slop class missing from catalog.
